// CRUD
/**
 * Create a new Employee in db
 * @param {String} firstName
 * @param {String} lastName
 * @param {number} roleId
 * @param {number} managerId
 */
function createEmployee(name, firstName, lastName, roleId, managerId) {
  //
}

/**
 * getting all Employees from db
 */

function getEmployees() {
  //
}

/**
 * Create a new Employee in db
 * @param {number} id
 * @param {number} newRole
 */
function updateEmployee(id, newRole) {}

module.exports = {
  createEmployee,
  getEmployees,
  updateEmployee,
};
